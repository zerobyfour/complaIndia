var config = {};

config.db_url = "localhost";
config.dp_port = "";
config.PORT = 3000;

config.AWS = {
		accessKeyId : "******",
		secretAccessKey : "*****",
		region : "****",
		bucket : "complaindia.com"
};

config.GMap = {
		key : "****"
};

module.exports = config;
